% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot_tempssm.R
\name{plot_tempssm_components}
\alias{plot_tempssm_components}
\title{Plot component estimates for tempssm objects}
\usage{
plot_tempssm_components(
  x,
  component = NULL,
  ci = TRUE,
  ci_level = 0.95,
  nrow = NULL,
  ncol = NULL,
  ...
)
}
\arguments{
\item{x}{An object returned by \code{tempssm()}.}

\item{component}{Character vector specifying one to four components to plot.
One of \code{"level"}, \code{"drift"}, \code{"season"}, or \code{"ar"}.
Values must be unique and are displayed in the supplied order. If
\code{NULL} (default), all components included in the fitted model are
plotted. Non-seasonal models omit the seasonal panel by default.}

\item{ci}{Logical; if TRUE, pointwise confidence intervals are returned.}

\item{ci_level}{Numeric confidence level between 0 and 1 (default: 0.95).}

\item{nrow, ncol}{Optional positive integers specifying the facet layout when
multiple components are plotted. When both are \code{NULL}, the layout is
selected automatically; four components use a 2 by 2 layout. These
arguments are ignored when one component is selected.}

\item{...}{Additional arguments passed to the corresponding
\code{autoplot_*()} function.}
}
\value{
A \code{ggplot} object. Multiple components are represented as facets with
free y-axis scales and component-specific units in the facet labels.
}
\description{
Explicit helper for plotting estimated components of a model fitted by
\code{tempssm()}. This function returns the same \code{ggplot} object as
\code{autoplot.tempssm()} and is useful when a descriptive function name is
preferred in scripts or examples.
}
\examples{
\dontrun{
data(sst_sim)
res <- tempssm(sst_sim)

# Explicit component-plot helper
plot_tempssm_components(res)

# Select components
plot_tempssm_components(res, component = c("level", "drift"))
}
}
